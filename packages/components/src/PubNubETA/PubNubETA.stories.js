import React from 'react';

import {ORDER_STATUS, orderStatusTime } from '@jmsstudiosinc/order';

import vendorMockData from '../OrderList/vendorMockData.json';


/* export default {
  title: 'packages/PubNubETA',
};

export const ShipCountDown = () => (
  <PubNubETA
    orderID={vendorMockData[1].id}
    role={"vendor"}
    status={vendorMockData[1].status}
    deliveryMethod={vendorMockData[1].deliveryMethod}
    eta={vendorMockData[1].eta.duration.value}
    deliveryTime={vendorMockData[1].eta?.deliveryTime?.value}
    restaurantAcceptedTime={new Date().getTime()}
    restaurantAcceptedTime={vendorMockData[1][orderStatusTime(ORDER_STATUS.restaurantAccepted)]
        ?.toDate()
        ?.getTime()} 
  />
) */




